version: v2
managed:
  enabled: true

plugins:
  # go-sdk
  - remote: buf.build/protocolbuffers/go
    out: ../api/go-sdk
    opt: paths=source_relative
  - remote: buf.build/connectrpc/go
    out: ../api/go-sdk
    opt: paths=source_relative

inputs:
  - directory: protos
